# sudo docker container ls -a
# sudo docker images

# # docker kill $(docker ps -q)
# # docker rm $(docker ps -a -q)
# # docker rmi $(docker images -q)
# docker rmi -f $(docker images -q)
# ls -la ../
# ls ~/projects/Work/
# rm -rf ~/projects/Work/smartella/*
# rm -rf ~/wdir/platform/app/backups/*